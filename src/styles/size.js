import { BREAK_POINT } from "./breakPoint";

/**
 * @description
 * Specify if component sizes are used in duplicate
 */

export const SIZE = {
  pageMaxWidth: BREAK_POINT.lg,
  quizMaxWidth: BREAK_POINT.sm,
};
